let movie: string = 'abc';
movie.length;
// movie = 12;

movie.toUpperCase();

let age: number = 12;

// age = "twio"

let isValid: boolean;

isValid = false;

console.log(isValid.valueOf());


let movieName: string;

for (const movie of movies) {
  if (movie === '1') {
    movieName = 'abc';
  }
}
